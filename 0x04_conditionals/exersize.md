## 1. create a simple password prompter for the user, which reads password from the user and checks whether that is 
##    equal to the password: 'bashisfun' and display a appropriate success/error message.

## 2.1 create a script to check whether a file exists or not in the current directory.
## 2.2 improve the previous script in 2.1 so the script by letting the user to provide a directory.

## 3. create an script check whether a program is installed on your operating system or not.
