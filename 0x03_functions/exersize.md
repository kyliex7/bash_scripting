## 1. create a function that prints all the arguments provided to the script.

## 2. try to implement a simple argument parser.(iydk wth is arg. parse. -- simply use google) (optional ofc).

## 3. implement a function that echoes something for number of times which is provided via input(recall user io).

## 4. create a function that lists all the contents in a user provided directory(hint: use ls with cmd subsitution).
