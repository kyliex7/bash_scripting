## 1. create a script that prints all the passed parameters to the script with their corresponding
##    index.
###     example output:
```
$ ./script deadbeef deadcode deadfood deaddood
    index 1: deadbeef
    index 2: deadcode
    index 3: deadfood
    index 4: deaddood
```

## 2. create a script checks whether a file exists on a user provided directory and print "file doesn't exists" 
##    until it does exists in that dir.
